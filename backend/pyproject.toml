[project]
name = "complyai"
version = "0.1.0"
description = "AI-powered compliance review for fintech marketing"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = [
    "fastapi",
    "pydantic",
    "uvicorn",
]
